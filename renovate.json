{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "pinDigests": true,
  "packageRules": [
    {
      "description": "Flag dependencies where the currently-used version is over 1 years old — likely abandoned or unmaintained.",
      "matchCurrentAge": "> 1 year",
      "addLabels": ["stale-dependency"]
    },
    {
      "matchManagers": ["mise"],
      "pinDigests": false
    },
    {
      "matchDepNames": ["jdx/mise"],
      "groupName": "mise"
    },
    {
      "matchDepNames": ["pgautoupgrade/pgautoupgrade", "postgres", "docker.io/library/postgres"],
      "groupName": "postgres"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/scripts/dccd\\.sh$/"
      ],
      "matchStrings": [
        "# renovate: datasource=github-releases depName=(?<depName>\\S+)\\nSOPS_VERSION=\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "github-releases"    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/.github/workflows/.+\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=github-releases depName=(?<depName>\\S+)\\n\\s+version: (?<currentValue>[^\\s]+)"
      ],
      "datasourceTemplate": "github-releases"    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/\\.mise\\.toml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=github-releases depName=(?<depName>\\S+)\\nmin_version = \"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "github-releases"    }
  ]
}
