---
services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.12.1@sha256:9627769818fbfb14147d3e633e57cef9c27c0c5f07585f5a1d6c3d3425b3b33c
    container_name: homepage
    env_file:
      - .env # Decrypted from secret.sops.env
      - ../shared/env/tz.env
    depends_on:
      homepage-docker-proxy:
        condition: service_healthy
    environment:
      - HOMEPAGE_ALLOWED_HOSTS=apps.${DOMAINNAME}
    restart: always
    networks:
      - homepage-frontend
      - homepage-backend
    mem_limit: ${MEM_LIMIT:-300m}
    pids_limit: 100
    security_opt:
      - no-new-privileges=true
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - ./config:/app/config
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      # Traefik
      - "traefik.enable=true"
      - "traefik.docker.network=homepage-frontend"
      ## HTTP Routers
      - "traefik.http.routers.homepage-rtr.rule=Host(`apps.${DOMAINNAME}`)"
      ## Middlewares
      - "traefik.http.routers.homepage-rtr.middlewares=chain-no-auth@file"
      ## HTTP Services
      - "traefik.http.routers.homepage-rtr.service=homepage"
      - "traefik.http.services.homepage.loadbalancer.server.port=3000"

  # LinuxServer socket-proxy runs as root by design (requires Docker socket access).
  # It does not support custom users, mods, or scripts.
  # See: https://github.com/linuxserver/docker-socket-proxy
  homepage-docker-proxy:
    image: lscr.io/linuxserver/socket-proxy:version-3.2.14-r0@sha256:56dde3144ef213d2df1ee3d600baad521bd8b8c2bf844672719b07363606f4c2
    container_name: homepage-docker-proxy
    env_file:
      - ../shared/env/tz.env
    environment:
      - CONTAINERS=1 # Allow access to viewing containers
      - POST=0 # Disallow any POST operations (effectively read-only)
    restart: always
    networks:
      - homepage-backend
    mem_limit: ${PROXY_MEM_LIMIT:-50m}
    pids_limit: 50
    security_opt:
      - no-new-privileges=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    read_only: true
    tmpfs:
      - /run
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:2375/_ping"]
      interval: 5s
      timeout: 5s
      retries: 3

networks:
  homepage-frontend:
    name: homepage-frontend
    driver: bridge
  homepage-backend:
    name: homepage-backend
    driver: bridge
    internal: true
