---
services:
  # Cloudflare Tunnel agent — exposes services to the internet via Cloudflare's
  # edge network without opening inbound ports on the host. Routing rules (which
  # hostname maps to which local service) are managed centrally in the Cloudflare
  # Zero Trust dashboard.
  #
  # No dedicated UID/GID: the official image already runs as nonroot (65532) and
  # this service has zero writable volumes, so there is no file ownership to manage.
  # No init container needed for the same reason.
  cloudflared:
    image: docker.io/cloudflare/cloudflared:2026.3.0@sha256:6b599ca3e974349ead3286d178da61d291961182ec3fe9c505e1dd02c8ac31b0
    container_name: cloudflared
    command:
      - "tunnel"
      - "--no-autoupdate"
      - "--metrics"
      - "0.0.0.0:2000"
      - "run"
    env_file:
      - path: .env # Decrypted from secret.sops.env
        required: false
      - path: ../shared/env/tz.env
    deploy:
      update_config:
        order: start-first
        failure_action: rollback
      restart_policy:
        condition: on-failure
        max_attempts: 3
        window: 120s
    networks:
      - hadiscover-frontend
    mem_limit: ${MEM_LIMIT:-256m}
    pids_limit: 100
    security_opt:
      - no-new-privileges=true
    cap_drop:
      - ALL
    read_only: true
    healthcheck:
      test:
        - "CMD"
        - "cloudflared"
        - "tunnel"
        - "--metrics"
        - "0.0.0.0:2000"
        - "ready"
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      # Homepage
      - homepage.group=Infrastructure
      - homepage.name=Cloudflared
      - homepage.icon=cloudflare.svg
      - homepage.description=Cloudflare Tunnel Agent

networks:
  # Created by hadiscover's compose.yaml. Because dccd.sh deploys apps
  # alphabetically, cloudflared (c) starts before hadiscover (h). On a
  # first deploy the network may not exist yet — re-run the deployment
  # to resolve.
  hadiscover-frontend:
    name: hadiscover-frontend
    external: true
