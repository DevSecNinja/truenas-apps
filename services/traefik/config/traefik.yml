---
api:
  dashboard: true

ping: {}

global:
  checkNewVersion: false
  sendAnonymousUsage: false

entryPoints:
  http:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: https
          scheme: https
          permanent: true
  https:
    address: ":443"
    http:
      tls: {}
  # Internal-only entrypoint for Gatus health checks. Not published to the host
  # (no ports: entry in compose.yaml), so it is only reachable from containers
  # that share a Docker network with Traefik. An entrypoint-level ipAllowList
  # restricts access to the gatus-frontend subnet (172.30.100.0/29), ensuring
  # no other container can use this entrypoint to bypass forward-auth.
  monitoring:
    address: ":8444"
    http:
      middlewares:
        - monitoring-ipallowlist@file
  # TCP entrypoint for MQTT (Mosquitto). Plain TCP — no TLS, so HostSNI(*) is
  # required as the router rule. Published on the host via compose.yaml ports:.
  mqtt:
    address: ":1883"
  # TCP entrypoint for DNS-over-TCP (AdGuard Home). HostSNI(*) router rule.
  dns-tcp:
    address: ":53"
  # UDP entrypoint for DNS-over-UDP (AdGuard Home). UDP routers have no rule
  # matching — every packet on this entrypoint is forwarded to AdGuard.
  dns-udp:
    address: ":53/udp"

metrics:
  prometheus:
    buckets:
      - 0.1
      - 0.3
      - 1.2
      - 5.0
      - 10.0

providers:
  docker:
    endpoint: tcp://traefik-docker-proxy:2375
    exposedByDefault: false
  file:
    directory: "/rules"
    watch: true

certificatesResolvers:
  dns-cloudflare:
    acme:
      # caServer: "https://acme-staging-v02.api.letsencrypt.org/directory" # LetsEncrypt Staging Server - uncomment when testing
      storage: "/acme/acme.json"
      dnsChallenge:
        provider: "cloudflare"
        resolvers:
          - "1.1.1.1:53"
          - "1.0.0.1:53"

accessLog:
  filters:
    statusCodes:
      - "400-599"
  fields:
    headers:
      names:
        User-Agent: keep
        Authorization: drop
        Content-Type: keep

log:
  level: INFO
