---
name: Assign issue to CODEOWNERS

on:
  workflow_dispatch: # checkov:skip=CKV_GHA_7: Not a build workflow — input is an issue number for manual assignment
    inputs:
      issue-number:
        description: "Issue number to assign (leave empty for all open issues)"
        required: false
        type: number
  issues:
    types:
      - opened

permissions:
  contents: read

jobs:
  assign:
    uses: DevSecNinja/.github/.github/workflows/assign-issue-to-codeowners.yml@76bb084c3dd335be7cc5cfffead44376120eb68c # main
    with:
      issue-number: ${{ inputs.issue-number || 0 }}
    permissions:
      contents: read
      issues: write
