---
name: Config Sync

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    uses: DevSecNinja/.github/.github/workflows/config-sync.yml@e744bc05bb096d6356cc913900ff62482e656046 # main
    permissions:
      contents: write
      pull-requests: write
